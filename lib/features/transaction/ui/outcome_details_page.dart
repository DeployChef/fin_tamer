import 'package:flutter/material.dart';

class OutcomeDetailsPage extends StatelessWidget {
  const OutcomeDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("OUTCOME DETAILS"),
      ),
    );
  }
}
